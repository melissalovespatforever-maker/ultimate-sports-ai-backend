[phases.setup]
nixPkgs = ["nodejs_24", "npm-9_x", "postgresql_16"]

[phases.install]
cmds = ["cd backend && npm install --production"]

[phases.build]
cmds = []

[phases.release]
cmds = ["node backend/scripts/run-migration.js"]

[start]
cmd = "cd backend && npm start"
