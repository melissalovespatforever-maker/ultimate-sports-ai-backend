[phases.setup]
nixPkgs = ["nodejs-24_x", "postgresql_16"]

[phases.install]
cmds = ["cd backend && npm install --production"]

[phases.build]
cmds = ["cd backend && npm run migrate"]

[start]
cmd = "cd backend && npm start"
